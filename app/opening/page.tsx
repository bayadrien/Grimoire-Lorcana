"use client";

import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import AppHeader from "app/components/AppHeader";
import { CHAPTERS_NAMES_FR } from "@/lib/chapters-fr";

type Place = { id: string; name: string; city?: string | null; kind: string };
type Session = { id: string; title?: string | null; provenanceType: string; provenanceNote?: string | null; place?: Place | null; _count: { openings: number } };
const chapters = Array.from({ length: 13 }, (_, index) => ({ id: index + 1, image: `/chapters/ch${index + 1}.jpg` }));
const boostersByChapter: Record<number, string[]> = Object.fromEntries(chapters.map(({ id }) => [id, [1, 2, 3].map((booster) => `/boosters/ch${id}-${booster}.jpg`)]));
const origins = ["Booster individuel", "Display", "Coffret / Gift Set", "Starter Deck", "Lot", "Avant-première", "Gain de tournoi", "Événement", "Cadeau", "Autre"];
const placeKinds = [{ value: "store", label: "Boutique" }, { value: "online", label: "En ligne" }, { value: "event", label: "Événement" }, { value: "other", label: "Autre" }];

export default function OpeningPage() {
  const router = useRouter();
  const [chapter, setChapter] = useState(13), [booster, setBooster] = useState(0);
  const [mode, setMode] = useState<"quick" | "single" | "new" | "existing">("quick");
  const [places, setPlaces] = useState<Place[]>([]), [sessions, setSessions] = useState<Session[]>([]), [sessionId, setSessionId] = useState("");
  const [title, setTitle] = useState(""), [placeName, setPlaceName] = useState(""), [city, setCity] = useState(""), [placeKind, setPlaceKind] = useState("store");
  const [provenanceType, setProvenanceType] = useState(origins[0]), [provenanceNote, setProvenanceNote] = useState(""), [paidPrice, setPaidPrice] = useState(""), [priceScope, setPriceScope] = useState<"product" | "booster">("product"), [comment, setComment] = useState("");
  const images = boostersByChapter[chapter] || [], selectedImage = images[booster], chapterName = CHAPTERS_NAMES_FR[String(chapter)] || `Chapitre ${chapter}`;
  const readyHref = useMemo(() => `/opening/live?chapter=${chapter}&booster=${encodeURIComponent(selectedImage || "")}`, [chapter, selectedImage]);

  useEffect(() => { const userId = localStorage.getItem("activeUser") || "adrien"; fetch(`/api/opening/metadata?userId=${userId}`, { cache: "no-store" }).then((r) => r.ok ? r.json() : { places: [], sessions: [] }).then((data) => { setPlaces(Array.isArray(data.places) ? data.places : []); setSessions(Array.isArray(data.sessions) ? data.sessions : []); }).catch(() => undefined); }, []);
  const usePlace = (place: Place) => { setPlaceName(place.name); setCity(place.city || ""); setPlaceKind(place.kind || "store"); };
  const selectedSession = sessions.find((session) => session.id === sessionId), detailsVisible = mode === "new" || mode === "single";
  function startOpening() {
    const metadata = mode === "existing" && sessionId ? { sessionId } : mode === "new" ? { createSession: true, title, place: placeName ? { name: placeName, city, kind: placeKind } : null, provenanceType, provenanceNote, paidPrice, priceScope, comment } : mode === "single" ? { place: placeName ? { name: placeName, city, kind: placeKind } : null, provenanceType, provenanceNote, paidPrice, priceScope, comment } : null;
    if (metadata) sessionStorage.setItem("grimoire-opening-draft", JSON.stringify(metadata)); else sessionStorage.removeItem("grimoire-opening-draft");
    router.push(readyHref);
  }

  return <main className="opening-page"><AppHeader /><div className="wrap">
    <section className="hero"><div><p className="eyebrow">ATELIER D’OUVERTURE</p><h1>Quel secret<br/><em>vas-tu révéler&nbsp;?</em></h1><p>Choisis ton chapitre, ton booster et, si tu le souhaites, garde la trace de son histoire.</p></div><div className="hero-pack">{selectedImage ? <img src={selectedImage} alt="Booster sélectionné" /> : <span>🎁</span>}<i>✦</i></div></section>
    <section className="steps"><span className="done"><b>1</b> Chapitre</span><i /><span className="done"><b>2</b> Booster</span><i /><span className="done"><b>3</b> Infos</span><i /><span><b>4</b> Ouverture</span></section>
    <section className="picker"><div className="section-head"><div><p className="eyebrow">ÉTAPE 1</p><h2>Choisis un chapitre</h2></div><span>Chapitre <b>{chapter}</b> · {chapterName}</span></div><div className="chapter-strip">{chapters.map((item) => <button key={item.id} className={chapter === item.id ? "selected" : ""} onClick={() => { setChapter(item.id); setBooster(0); }}><img src={item.image} alt={`Chapitre ${item.id}`} /><b>{item.id}</b><small>{CHAPTERS_NAMES_FR[String(item.id)] || "Lorcana"}</small></button>)}</div></section>
    <section className="picker packs"><div className="section-head"><div><p className="eyebrow">ÉTAPE 2</p><h2>Choisis ton booster</h2></div><span>3 visuels disponibles</span></div><div className="pack-grid">{images.map((image, index) => <button key={image} className={booster === index ? "selected" : ""} onClick={() => setBooster(index)}><div><img src={image} alt={`Booster ${index + 1} du chapitre ${chapter}`} />{booster === index && <i>✓</i>}</div><span>Booster {index + 1}</span></button>)}</div></section>
    <section className="metadata"><div className="section-head"><div><p className="eyebrow">ÉTAPE 3 · FACULTATIVE</p><h2>L’histoire de ce booster</h2><p>Ajoute une provenance détaillée ou passe directement à l’ouverture.</p></div></div>
      <div className="mode-grid"><button className={mode === "quick" ? "active" : ""} onClick={() => setMode("quick")}><b>⚡ Ouverture rapide</b><small>Aucune information à remplir</small></button><button className={mode === "single" ? "active" : ""} onClick={() => setMode("single")}><b>🎁 Booster seul</b><small>Lieu, origine et note facultatifs</small></button><button className={mode === "new" ? "active" : ""} onClick={() => setMode("new")}><b>✨ Nouvelle session</b><small>Pour plusieurs boosters de la même soirée</small></button><button className={mode === "existing" ? "active" : ""} onClick={() => setMode("existing")}><b>↻ Continuer une session</b><small>{sessions.length ? `${sessions.length} session${sessions.length > 1 ? "s" : ""} récente${sessions.length > 1 ? "s" : ""}` : "Aucune session"}</small></button></div>
      {mode === "existing" && <div className="existing">{sessions.length ? <>{sessions.map((session) => <button key={session.id} className={sessionId === session.id ? "selected" : ""} onClick={() => setSessionId(session.id)}><b>{session.title || session.provenanceNote || session.provenanceType}</b><small>{session.place ? `📍 ${session.place.name}${session.place.city ? ` · ${session.place.city}` : ""}` : "Sans lieu"} · {session._count.openings} booster{session._count.openings > 1 ? "s" : ""}</small></button>)}{selectedSession && <p>Les informations de <b>{selectedSession.title || selectedSession.provenanceType}</b> seront reprises automatiquement.</p>}</> : <p className="empty-meta">Crée ta première session ci-dessus : elle sera disponible dès que ce booster sera enregistré.</p>}</div>}
      {detailsVisible && <div className="detail-grid">{mode === "new" && <label className="full"><span>Nom de la session <i>facultatif</i></span><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Ex. Soirée Opening avec Angèle" /></label>}<div className="full recent"><span>📍 Lieu d’achat <i>facultatif</i></span>{places.length ? <div>{places.slice(0, 5).map((place) => <button key={place.id} onClick={() => usePlace(place)}>{place.name}{place.city ? ` · ${place.city}` : ""}</button>)}</div> : null}</div><label><span>Nom du lieu ou site</span><input value={placeName} onChange={(e) => setPlaceName(e.target.value)} placeholder="Cardmarket ou Boutique X" /></label><label><span>Ville <i>facultatif</i></span><input value={city} onChange={(e) => setCity(e.target.value)} placeholder="Dunkerque" /></label><label><span>Type de lieu</span><select value={placeKind} onChange={(e) => setPlaceKind(e.target.value)}>{placeKinds.map((kind) => <option key={kind.value} value={kind.value}>{kind.label}</option>)}</select></label><label><span>📦 Provenance</span><select value={provenanceType} onChange={(e) => setProvenanceType(e.target.value)}>{origins.map((origin) => <option key={origin}>{origin}</option>)}</select></label><label className="full"><span>Précision <i>facultatif</i></span><input value={provenanceNote} onChange={(e) => setProvenanceNote(e.target.value)} placeholder="Coffret Stitch chapitre 13 ou tournoi Lorcana Dunkerque" /></label><label><span>💶 Prix payé <i>facultatif</i></span><input inputMode="decimal" value={paidPrice} onChange={(e) => setPaidPrice(e.target.value)} placeholder="29,99" /></label><label><span>Ce prix correspond à</span><select value={priceScope} onChange={(e) => setPriceScope(e.target.value as "product" | "booster")}><option value="product">Produit / coffret / lot entier</option><option value="booster">Ce booster</option></select></label><label className="full"><span>💬 Note <i>facultatif</i></span><textarea value={comment} onChange={(e) => setComment(e.target.value)} placeholder="Ouvert avec Angèle pendant notre soirée Lorcana." rows={2} /></label></div>}
    </section>
    <section className="ready"><div><p className="eyebrow">TON CHOIX</p><h2>Chapitre {chapter} · {chapterName}</h2><p>{mode === "quick" ? "Tout est prêt pour une ouverture express." : mode === "existing" ? "Cette ouverture rejoindra la session sélectionnée." : "Ces informations seront enregistrées avec ton booster, sans ralentir l’ouverture."}</p></div><button onClick={startOpening}>Continuer l’Opening <b>→</b></button></section>
  </div><style jsx>{styles}</style></main>;
}

const styles = `.opening-page{min-height:100vh;background:radial-gradient(circle at 7% 7%,#fff0c9,transparent 25rem),radial-gradient(circle at 95% 39%,#e6ddff,transparent 29rem),#f8f5ef;color:#2c2638;padding-bottom:65px}.wrap{max-width:1160px;margin:auto;padding:27px 18px}.hero{min-height:275px;display:flex;align-items:center;justify-content:space-between;gap:35px;padding:35px 43px;border-radius:30px;color:#fff;background:linear-gradient(120deg,#251d43,#473277 59%,#7d5bad);box-shadow:0 22px 49px #3525653d;overflow:hidden;position:relative}.hero:after{content:'✦';position:absolute;right:29%;top:-90px;font:300px Georgia;color:#ffffff10}.eyebrow{font-size:10px;font-weight:900;letter-spacing:.14em;color:#8c8494;margin:0}.hero .eyebrow{color:#e3daf8}.hero h1{margin:11px 0;font-size:clamp(39px,5vw,56px);letter-spacing:-.07em;line-height:.95}.hero h1 em{font-family:Georgia;font-weight:400;color:#ffd168}.hero p:not(.eyebrow){max-width:550px;margin:0;color:#dfd7f1;font-size:14px;line-height:1.55}.hero-pack{width:132px;position:relative;z-index:1;transform:rotate(5deg);filter:drop-shadow(0 18px 21px #0004)}.hero-pack img{width:100%;border-radius:13px}.hero-pack i{position:absolute;right:-24px;top:-22px;color:#ffe18f;font-size:35px;font-style:normal}.steps{display:flex;align-items:center;justify-content:center;gap:9px;margin:19px 0;color:#958c9d;font-size:11px;font-weight:800}.steps span{display:flex;align-items:center;gap:6px}.steps b{width:22px;height:22px;display:grid;place-items:center;border-radius:50%;background:#e7e1ea}.steps .done{color:#5a4283}.steps .done b{background:#70539f;color:#fff}.steps>i{height:1px;width:54px;background:#dfd9e4}.picker,.metadata{padding:23px;margin-top:15px;border:1px solid #e9e3ec;border-radius:22px;background:#ffffffdc;box-shadow:0 8px 22px #30254b0e}.section-head{display:flex;justify-content:space-between;align-items:end;gap:14px;margin-bottom:15px}.section-head h2{font-size:22px;letter-spacing:-.05em;margin:6px 0 0}.section-head p:not(.eyebrow){margin:5px 0 0;color:#766d7c;font-size:12px}.section-head>span{padding:7px 9px;border-radius:99px;background:#f5f2f8;color:#877e90;font-size:10px}.section-head>span b{color:#513b7c}.chapter-strip{display:grid;grid-template-columns:repeat(13,1fr);gap:7px;overflow-x:auto;padding:3px}.chapter-strip button,.pack-grid button{border:0;background:transparent;cursor:pointer;color:#4a4254;font:inherit;text-align:left}.chapter-strip button{min-width:66px;position:relative;padding:4px;border-radius:10px;transition:.18s}.chapter-strip button:hover{transform:translateY(-3px)}.chapter-strip img{width:100%;aspect-ratio:.84;object-fit:cover;border-radius:7px;display:block;filter:saturate(.75);opacity:.73}.chapter-strip b{position:absolute;top:8px;left:8px;width:19px;height:19px;display:grid;place-items:center;border-radius:50%;background:#191229bf;color:#fff;font-size:10px}.chapter-strip small{display:block;margin:5px 2px 0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;font-size:8px;color:#8c8394}.chapter-strip .selected{background:#eee7f8;box-shadow:0 0 0 2px #7457a5}.chapter-strip .selected img{filter:none;opacity:1}.packs{background:linear-gradient(135deg,#fff,#fdfaff)}.pack-grid{display:flex;justify-content:center;gap:24px}.pack-grid button{padding:9px;border-radius:16px;text-align:center;transition:.2s}.pack-grid button:hover{transform:translateY(-5px)}.pack-grid button>div{position:relative;width:150px;border-radius:12px;overflow:hidden;box-shadow:0 10px 21px #271d401f}.pack-grid img{width:100%;aspect-ratio:.69;object-fit:cover;display:block}.pack-grid i{position:absolute;right:8px;top:8px;width:28px;height:28px;display:grid;place-items:center;border-radius:50%;background:#f9d365;color:#3f2e11;font-style:normal;font-weight:900}.pack-grid button>span{display:block;margin-top:8px;font-size:11px;font-weight:900}.pack-grid .selected{background:#f2ebfc;box-shadow:0 0 0 2px #7658a4}.metadata{background:linear-gradient(135deg,#fffdf8,#f9f5ff)}.mode-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.mode-grid button,.existing button{padding:13px;border:1px solid #e5ddea;border-radius:13px;background:#fff;text-align:left;cursor:pointer;color:#4a4056}.mode-grid button.active,.existing button.selected{border-color:#7151a1;background:#f0eafe;box-shadow:0 0 0 2px #7151a11c}.mode-grid b,.mode-grid small{display:block}.mode-grid b{font-size:12px}.mode-grid small{margin-top:4px;color:#887e91;font-size:10px;line-height:1.3}.existing{display:grid;gap:8px;margin-top:13px}.existing button{display:grid;gap:3px}.existing b{font-size:12px}.existing small,.existing p,.empty-meta{color:#81758f;font-size:11px}.detail-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-top:15px}.detail-grid label,.detail-grid .recent{display:grid;gap:5px}.detail-grid .full{grid-column:1/-1}.detail-grid span{font-size:10px;font-weight:800;color:#6d6279}.detail-grid i{font-style:normal;color:#9b91a4;font-weight:500}.detail-grid input,.detail-grid select,.detail-grid textarea{width:100%;box-sizing:border-box;border:1px solid #e3dce9;border-radius:10px;padding:10px 11px;background:#fff;color:#403648;font:12px inherit;outline-color:#7960a4}.detail-grid textarea{resize:vertical}.recent>div{display:flex;gap:6px;flex-wrap:wrap}.recent button{border:1px solid #e3d9ee;border-radius:99px;background:#f6f1fb;padding:6px 8px;color:#67517d;font-size:10px;cursor:pointer}.ready{display:flex;align-items:center;justify-content:space-between;gap:25px;margin-top:16px;padding:23px 27px;border:1px solid #eadbb7;border-radius:22px;background:linear-gradient(115deg,#fff9e8,#faf7ff)}.ready h2{font-size:21px;letter-spacing:-.04em;margin:5px 0}.ready p:not(.eyebrow){max-width:640px;margin:0;color:#766d7c;font-size:12px;line-height:1.5}.ready button{display:inline-flex;gap:10px;align-items:center;padding:13px 16px;border:0;border-radius:12px;background:#f3c85a;color:#43310e;font:900 12px inherit;white-space:nowrap;box-shadow:0 8px 16px #ca99252e;cursor:pointer}.ready b{font-size:18px}@media(max-width:800px){.chapter-strip{grid-template-columns:repeat(7,1fr)}.hero{padding:31px}.pack-grid{justify-content:flex-start;overflow-x:auto;padding-bottom:4px}.mode-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:560px){.wrap{padding:15px 12px}.hero{min-height:235px;padding:27px 21px}.hero h1{font-size:38px}.hero-pack{width:88px}.steps{font-size:8px;gap:5px}.steps>i{width:12px}.picker,.metadata{padding:18px 12px;border-radius:18px}.section-head h2{font-size:19px}.section-head>span{display:none}.chapter-strip{grid-template-columns:repeat(5,1fr);gap:6px}.chapter-strip button{min-width:0}.pack-grid{gap:10px}.pack-grid button>div{width:108px}.mode-grid{gap:7px}.mode-grid button{padding:10px}.detail-grid{grid-template-columns:1fr}.detail-grid .full{grid-column:auto}.ready{align-items:flex-start;flex-direction:column;padding:20px;border-radius:18px}.ready button{width:100%;justify-content:center}}`;
